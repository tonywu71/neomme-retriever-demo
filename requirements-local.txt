# Running the app outside Spaces. The Space build spec omits `gradio` and `spaces` because the runtime supplies
# them; a local venv has to install them itself, at the versions the Space actually runs.
-r requirements.txt
gradio==6.22.0
spaces>=0.30
# Gradio 6.22 calls GZipResponder without the keyword Starlette 1.4 made mandatory.
starlette==1.3.1
